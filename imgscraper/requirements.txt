requests>=2.31
beautifulsoup4>=4.12
lxml>=4.9
Pillow>=10.0
tqdm>=4.66

# Optional extra — only needed for the --playwright flag (JavaScript-heavy sites).
# After installing, also run: playwright install chromium
# playwright>=1.42
